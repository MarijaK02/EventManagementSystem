/*package mk.ukim.finki.eventapp.service;

import mk.ukim.finki.eventapp.model.Rate;

import java.util.List;

public interface RateService {

    List<Rate> getAllRates();
    Rate addRate(Rate rate);
    Rate updateRate(Rate rate);
    void deleteRate(Long id);
    List<Rate> findRatesByEventId(Long eventId);
    List<Rate> findRatesByUserId(Long userId);
}

 */
